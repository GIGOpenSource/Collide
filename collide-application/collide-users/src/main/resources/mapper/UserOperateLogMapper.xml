<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gig.collide.users.infrastructure.mapper.UserOperateLogMapper">

    <!-- 根据用户ID查询操作日志 -->
    <select id="findByUserIdLimit" resultType="com.gig.collide.users.domain.entity.UserOperateLog">
        SELECT * FROM t_user_operate_log 
        WHERE user_id = #{userId} 
        ORDER BY create_time DESC 
        LIMIT #{limit}
    </select>

    <!-- 根据操作类型统计数量 -->
    <select id="countByUserIdAndOperateType" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_user_operate_log 
        WHERE user_id = #{userId} AND operate_type = #{operateType}
    </select>

</mapper> 