# =====================================
# Collide Business Bootstrap 配置
# 参考 nft-turbo-app 的配置架构
# =====================================
spring:
  application:
    name: @application.name@
  config:
    # 参考 nft-turbo-app 的配置导入方式
    # 这里爆红可以直接忽略，没有任何影响
    import: classpath:base.yml,classpath:config.yml