<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gig.collide.follow.infrastructure.mapper.FollowMapper">

    <!-- 查询关注关系 -->
    <select id="selectByFollowerAndFollowed" resultType="com.gig.collide.follow.domain.entity.Follow">
        SELECT * FROM t_follow 
        WHERE follower_user_id = #{followerUserId} 
        AND followed_user_id = #{followedUserId}
    </select>

    <!-- 分页查询关注列表 -->
    <select id="selectFollowList" resultType="com.gig.collide.follow.domain.entity.Follow">
        SELECT * FROM t_follow 
        WHERE follower_user_id = #{userId} 
        AND status = 1
        <if test="followType != null">
            AND follow_type = #{followType}
        </if>
        ORDER BY created_time DESC
    </select>

    <!-- 分页查询粉丝列表 -->
    <select id="selectFollowerList" resultType="com.gig.collide.follow.domain.entity.Follow">
        SELECT * FROM t_follow 
        WHERE followed_user_id = #{userId} 
        AND status = 1
        ORDER BY created_time DESC
    </select>

    <!-- 统计关注数量 -->
    <select id="countFollowing" resultType="int">
        SELECT COUNT(*) FROM t_follow 
        WHERE follower_user_id = #{userId} 
        AND status = 1
    </select>

    <!-- 统计粉丝数量 -->
    <select id="countFollowers" resultType="int">
        SELECT COUNT(*) FROM t_follow 
        WHERE followed_user_id = #{userId} 
        AND status = 1
    </select>

</mapper> 