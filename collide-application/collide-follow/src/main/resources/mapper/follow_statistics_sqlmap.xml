<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gig.collide.follow.infrastructure.mapper.FollowStatisticsMapper">

    <!-- 增加关注数 -->
    <update id="incrementFollowingCount">
        UPDATE t_follow_statistics 
        SET following_count = following_count + 1,
            updated_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <!-- 减少关注数 -->
    <update id="decrementFollowingCount">
        UPDATE t_follow_statistics 
        SET following_count = GREATEST(following_count - 1, 0),
            updated_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <!-- 增加粉丝数 -->
    <update id="incrementFollowerCount">
        UPDATE t_follow_statistics 
        SET follower_count = follower_count + 1,
            updated_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <!-- 减少粉丝数 -->
    <update id="decrementFollowerCount">
        UPDATE t_follow_statistics 
        SET follower_count = GREATEST(follower_count - 1, 0),
            updated_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <!-- 初始化用户统计记录 -->
    <insert id="initUserStatistics">
        INSERT IGNORE INTO t_follow_statistics (user_id, following_count, follower_count, created_time, updated_time)
        VALUES (#{userId}, 0, 0, NOW(), NOW())
    </insert>

</mapper> 