# RocketMQ 配置
rocketmq:
  name-server: ${collide.turbo.rocketmq.url:localhost:9876}
  producer:
    group: collide-producer-group
    send-message-timeout: 3000
    retry-times-when-send-failed: 2
  consumer:
    group: collide-consumer-group 