dubbo:
  consumer:
    timeout: 3000
    check: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://${collide.turbo.nacos.server.url}?username=nacos&password=nacos
    parameters:
      namespace: ${collide.turbo.dubbo.nacos.namespace}
      group: ${collide.turbo.dubbo.nacos.group}
  application:
    name: ${spring.application.name}
    qos-enable: true
    qos-accept-foreign-ip: false
    serialize-check-status: DISABLE