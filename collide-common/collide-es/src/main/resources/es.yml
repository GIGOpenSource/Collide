# Elasticsearch 配置
elasticsearch:
  uris: ${collide.turbo.elasticsearch.url:localhost:9200}
  username: ${collide.turbo.elasticsearch.username:}
  password: ${collide.turbo.elasticsearch.password:}
  connection-timeout: 5s
  socket-timeout: 30s 