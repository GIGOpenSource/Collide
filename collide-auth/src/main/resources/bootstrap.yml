# Collide Auth Bootstrap 配置
# 简化认证系统启动配置
spring:
  application:
    name: collide-auth
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: classpath:base.yml,classpath:config.yml